class Main
    void main()
        std:ln("std:ln(boob);");

        std:ln("Enter text: ");
        x = input();

        std:ln("your text: " + x);

    endf
endc

if (1 < 2) do
    std:ln("Hello World");
endf